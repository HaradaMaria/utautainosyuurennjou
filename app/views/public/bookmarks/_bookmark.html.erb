<% if song.bookmarking_by?(current_user) %>
  <%= link_to song_bookmarks_path(song), method: :delete, remote: true do%>
    ♥
  <% end %>
<% else %>
  <%= link_to song_bookmarks_path(song), method: :post, remote: true do%>
    ♡
  <% end %>
<% end %>